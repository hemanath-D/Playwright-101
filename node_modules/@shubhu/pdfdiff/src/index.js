// Main Node.js entry point - provides file system support
import { extractPdfText, comparePdfs, compareTexts, formatDiff } from './node.js';

// Re-export all functionality
export { extractPdfText, comparePdfs, compareTexts, formatDiff };

export default { extractPdfText, comparePdfs, formatDiff };